# Telegram USB 자동화 테스트 — 결과 정리

> 최종 갱신: 2026-07-26 (진행 중)
> 대시보드(실시간): https://kye-jaemin.github.io/telegram-usb-auto-test/

## 1. 개요

USB로 연결한 실기기에서 아래 **14단계 시나리오**를 ADB + uiautomator2로 자동 수행하고, 결과를 누적 대시보드(GitHub Pages)로 공개.

| 단계 | 내용 |
|---|---|
| 1 | 텔레그램 실행 |
| 2 | 설정 > 내 계정 > 로그아웃 |
| 3 | 로그아웃 확인 |
| 4 | Recent 진입 후 텔레그램 종료 |
| 5 | 텔레그램 재실행 > 시작하기 |
| 6 | 로그인 화살표(국가코드 +82, 번호, "네" 확인) |
| 7 | 로그인 확인 (SMS 자동입력 / Google SSO) |
| 8 | 비행기모드 On |
| 9 | 5초 후 비행기모드 Off |
| 10 | ExpressVPN 실행 |
| 11 | ExpressVPN 서버 미국으로 변경 |
| 12 | 텔레그램 재실행 정상 동작 |
| 13 | "뚱재민"에게 "test" 전송 |
| 14 | ExpressVPN 서버 한국으로 변경 → 최종 Success |

**환경**: 삼성 갤럭시(테스트 진행 기기 SM-F971N 폴더블, Android 17, 1248×1972) / Windows + Python 3.12 + ADB / Telegram `org.telegram.messenger`, ExpressVPN `com.expressvpn.vpn`

## 2. 집계 결과 (진행 중)

| 지표 | 값 |
|---|---|
| 총 시도 | 60 |
| 성공 | 50 |
| 실패 | 10 |
| 성공률 | 83.3% |
| 14단계 완주 성공 | 50 |
| 성공 1회 평균 소요 | 173.6초 (~2분 54초) |

- 초기 SM-S928N(비폴더블)에서 하니스 검증 후, SM-F971N(폴더블)로 교체하여 본 테스트 진행.
- 각 실행마다 대시보드 자동 갱신·푸시.

## 3. 실패 분석 (10건)

| 유형 | 건수 | 원인 |
|---|---|---|
| 텔레그램 flood 쿨다운 | 6 | 짧은 시간 로그인 반복 → "너무 많이 시도하셨습니다. 나중에 다시 시도하세요" |
| 번호 입력 깨짐 | 일부 | `set_text`가 기존 값에 덧붙어 번호 오염(예: `10740435370`) → 로그인 실패 |
| 시작 전환 글리치 | 일부 | 루프 중간 재시작 시 앱 전환 타이밍 |

## 4. 발견한 이슈 & 해결

1. **번호 프리필 오염 버그(핵심)**
   - 로그아웃 후 번호칸에 `set_text` 시 기존 값이 안 지워져 자릿수가 깨짐 → 잘못된 번호로 로그인 실패.
   - **해결**: 입력 전 `clear_text()` + 값 검증/재시도. 이후 로그인 안정화.

2. **텔레그램 flood 쿨다운 (외부 제약)**
   - 로그아웃/재로그인을 반복하면 실제로 ~십수 회 후 SMS→이메일 인증 요구, 더 반복 시 로그인 완전 차단.
   - **대응**: (a) 이메일 인증 강제 시 **Google SSO 자동 로그인** 폴백, (b) 쿨다운도 **실패로 로깅 + 화면 캡처 저장**, (c) 쿨다운 감지 시 대기 후 자동 재시도.
   - **결론**: "매 회차 로그아웃/재로그인 300회"는 텔레그램 정책상 완주 어려움(주기적 쿨다운). 로그인 제외 시 빠른 완주 가능.

3. **폴더블 해상도 대응**: 하드코딩 좌표를 해상도 비율(`tap_ratio`)로 전환.

4. **화면 자동잠금**: 보안잠금으로 자동화 중단 → `svc power stayon usb`로 화면 유지.

5. **진단 로그 관련**: 텔레그램 릴리즈 빌드는 자체 로그를 logcat에 남기지 않음 → **문제 진단의 핵심은 스크린샷**. 실패 시 스크린샷 + logcat(시스템) + (활성화 시)텔레그램 디버그 로그를 덤프에 저장.

## 5. 산출물

- **테스트 하니스**: `run_test.py`, `tg_test/` (device/steps/report/config)
- **실시간 대시보드**: 총 시도/성공/실패/성공률 + 전체 14단계 시나리오 + 실행 이력(run id) + 실패 아티팩트
- **실패 아티팩트(공개)**: `docs/runs/<run_id>/` — 마스킹(블러) 스크린샷 + 로그
- **로컬 덤프(비공개)**: `results/dumps/<run_id>/` — 원본 스크린샷 + logcat, `cooldown-*/`에 쿨다운 에러 화면 증거

## 6. 현재 상태 / 다음

- 로그인 포함 모드로 300 목표 진행 중. 계정 rate-limit로 **쿨다운↔재개를 반복**하여 완주까지 장시간 소요.
- 빠른 300 완주가 필요하면 **로그인 제외 모드**(로그아웃/인 1회 후 나머지 반복)로 전환 가능.
